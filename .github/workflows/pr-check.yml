name: Run test commands

on:
  pull_request:
    branches: [ master ]

jobs:
  pr-check:
    uses: fizyk/actions-reuse/.github/workflows/shared-pr-check.yml@v5.0.0
    with:
      dependency-manager: uv

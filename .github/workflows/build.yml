name: Test build package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_call:

permissions: {}

jobs:
  build:
    uses: fizyk/actions-reuse/.github/workflows/shared-pypi.yml@42b61e053c1d70a61deb42b323c1a2229e2e7fe0 # v5.5.0
    with:
      dependency-manager: uv

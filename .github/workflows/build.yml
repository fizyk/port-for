name: Test build package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_call:

permissions: {}

jobs:
  build:
    uses: fizyk/actions-reuse/.github/workflows/shared-pypi.yml@ae6177442c58a91f60ffaf7f7a90a331aaf3b049 # v5.4.1
    with:
      dependency-manager: uv

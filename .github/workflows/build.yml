name: Test build package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_call:

permissions: {}

jobs:
  build:
    uses: fizyk/actions-reuse/.github/workflows/shared-pypi.yml@7eefbd6ffc6c6787ad3be6ce93dcf475e3ae99b5 # v5.6.0
    with:
      dependency-manager: uv

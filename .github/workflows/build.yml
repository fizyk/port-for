name: Test build package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_call:

jobs:
  build:
    uses: fizyk/actions-reuse/.github/workflows/shared-pypi.yml@c76d10cf9448b3a9afdbef29e727e0fd00b2f544 # v5.2.8
    with:
      dependency-manager: uv

name: Test build package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_call:

permissions: {}

jobs:
  build:
    uses: fizyk/actions-reuse/.github/workflows/shared-pypi.yml@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
    with:
      dependency-manager: uv
